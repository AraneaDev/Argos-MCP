#!/bin/sh
# The slow half of the gate: everything CI will run, run once before the push
# rather than discovered in a failed workflow ten minutes later.
#
# Deliberate exception (pushing a WIP branch for a second pair of eyes):
#     git push --no-verify
set -eu

npm run validate
